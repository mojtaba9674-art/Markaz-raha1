# 📸 راهنمای کامل افزودن و تغییر عکس‌های سایت مرکز رها

این راهنما قدم‌به‌قدم توضیح می‌دهد چطور عکس‌های واقعی مرکز را جایگزین عکس‌های نمونه کنید.

---

## 🗂️ ساختار پوشه‌های عکس

```
public/
└── images/
    ├── gallery/          ← عکس‌های محیط مرکز
    │   ├── therapy-room.jpg
    │   ├── art-workshop.jpg
    │   ├── group-room.jpg
    │   └── courtyard.jpg
    ├── 360/              ← عکس پانورامای ۳۶۰ درجه
    │   └── center-panorama.jpg
    ├── shop/             ← عکس محصولات فروشگاه خیریه
    │   ├── pottery.jpg
    │   ├── painting.jpg
    │   ├── kilim.jpg
    │   ├── mosaic-lamp.jpg
    │   └── basket.jpg
    └── team/             ← عکس‌های پرسنل (خودتان اضافه می‌کنید)
```

> ⚠️ **نکته مهم:** نام فایل‌ها حتماً با حروف انگلیسی و بدون فاصله باشد.
> مثال درست: `nargis-hoseini.jpg` — مثال غلط: `عکس نرگس.jpg`

---

## ۱️⃣ عکس‌های محیط مرکز (گالری)

**روش ساده (جایگزینی):**
عکس‌های واقعی مرکز را با همین نام‌ها جایگزین فایل‌های فعلی در `public/images/gallery/` کنید:

| فایل | محل نمایش | ابعاد پیشنهادی |
|---|---|---|
| `group-room.jpg` | کارت بزرگ اول (سالن گروه‌درمانی) | ۱۲۰۰×۹۰۰ |
| `therapy-room.jpg` | کارت دوم (اتاق مشاوره) | ۸۰۰×۶۰۰ |
| `art-workshop.jpg` | کارت سوم (کارگاه هنر) | ۸۰۰×۶۰۰ |
| `courtyard.jpg` | کارت پهن آخر (حیاط) | ۱۲۰۰×۶۰۰ |

**افزودن عکس بیشتر:**
فایل `src/components/Gallery.tsx` را باز کنید و به آرایه `photos` یک آیتم اضافه کنید:

```js
{
  src: '/images/gallery/new-photo.jpg',
  title: 'عنوان عکس',
  desc: 'توضیح کوتاه',
  icon: Camera,   // یا Users, Palette, Trees, Sofa
  span: '',       // برای کارت بزرگ: 'md:col-span-2'
},
```

---

## ۲️⃣ عکس ۳۶۰ درجه (بازدید مجازی)

**مشخصات عکس موردنیاز:**
- فرمت: **Equirectangular** (پانورامای کروی)
- نسبت ابعاد: حتماً **۲:۱** (مثلاً ۴۰۹۶×۲۰۴۸ پیکسل)
- فرمت فایل: JPG

**چطور عکس ۳۶۰ بگیرید؟**
1. با گوشی و اپلیکیشن‌هایی مثل **Google Street View** یا دوربین‌های ۳۶۰ (مثل Insta360 یا Ricoh Theta) از وسط سالن اصلی عکس پانوراما بگیرید
2. فایل خروجی را به نام `center-panorama.jpg` تغییر نام دهید
3. در مسیر `public/images/360/` جایگزین فایل فعلی کنید

---

## ۳️⃣ عکس‌های پرسنل (تیم)

**قدم اول:** پوشه `public/images/team/` بسازید و عکس هر نفر را با نام انگلیسی داخل آن بگذارید. ابعاد پیشنهادی: مربعی، حداقل ۴۰۰×۴۰۰.

**قدم دوم:** فایل `src/components/Team.tsx` را باز کنید و جلوی `photo` هر نفر مسیر عکسش را بنویسید:

```js
{
  name: 'دکتر نرجس حسینی',
  ...
  photo: '/images/team/nargis-hoseini.jpg',  // ← اینجا
},
```

> ✅ تا وقتی `photo` مقدار `null` داشته باشد، آواتار گرادیانی با حرف اول نام نمایش داده می‌شود. یعنی می‌توانید عکس‌ها را کم‌کم و هر وقت آماده شدند اضافه کنید.

**نام پیشنهادی فایل‌ها:**
- `nargis-hoseini.jpg`
- `omolbanin-roodbari.jpg`
- `atefe-maslami.jpg`
- `anita-rashidi.jpg`
- `farzaneh-mashayekhi.jpg`
- `leili-erfand.jpg`
- `shafagh-shahabi.jpg`

---

## ۴️⃣ محصولات فروشگاه خیریه (بدون محدودیت تعداد!)

**جایگزینی عکس محصول فعلی:**
عکس جدید را با همان نام در `public/images/shop/` جایگزین کنید. ابعاد پیشنهادی: ۸۰۰×۶۰۰ (نسبت ۴:۳).

**افزودن محصول جدید — بدون هیچ محدودیتی:**
فایل `src/components/CharityShop.tsx` را باز کنید و به آرایه `products` هر تعداد محصول که خواستید اضافه کنید؛ گرید فروشگاه به‌صورت خودکار با هر تعداد محصول سازگار می‌شود:

```js
{
  id: 6,                       // عدد یکتا و ترتیبی
  title: 'نام اثر',
  category: 'دسته‌بندی',
  price: '۳۵۰٬۰۰۰',           // قیمت به تومان با اعداد فارسی
  image: '/images/shop/new.jpg',
  creator: 'نام مستعار خالق',
  condition: 'مددجوی مرکز با تجربه‌ی ...',
  meaning: 'معنای اثر در یک جمله',
  story: 'قصه‌ی کامل اثر و خالقش...',
  materials: 'جنس و متریال',
  dimensions: 'ابعاد',
},
```

**✅ علامت‌گذاری اثر فروخته‌شده (خیلی مهم):**
اثر فروخته‌شده را **حذف نکنید!** فقط کافیست به آن `sold: true` اضافه کنید:

```js
{
  id: 4,
  title: 'فانوس «نورِ کنار هم»',
  ...
  sold: true,   // ← فقط همین یک خط!
},
```

با این کار به‌صورت خودکار:
- عکس اثر می‌ماند اما کمی سیاه‌وسفید می‌شود
- نشان بزرگ **«فروخته شد»** روی عکس نمایش داده می‌شود
- دکمه سفارش غیرفعال می‌شود
- اثر به **انتهای لیست** منتقل می‌شود (محصولات موجود همیشه اول دیده می‌شوند)
- در فیلتر «فروخته‌شده» بالای فروشگاه نمایش داده می‌شود

> 💡 نمایش آثار فروخته‌شده اعتبار فروشگاه را بالا می‌برد و به بازدیدکننده نشان می‌دهد که آثار واقعاً خریدار دارند!

---

## ۵️⃣ لوگوی تب مرورگر (Favicon)

در حال حاضر یک لوگوی برداری (قلب سبز با نبض) به‌صورت پیش‌فرض در تب مرورگر نمایش داده می‌شود.

**برای جایگزینی با لوگوی واقعی مرکز:**

### روش الف — لوگوی PNG (ساده‌تر)
1. لوگوی مرکز را به فرمت PNG **مربعی** با ابعاد **۵۱۲×۵۱۲** پیکسل و پس‌زمینه شفاف آماده کنید
2. آن را با نام `favicon.png` در پوشه `public/` قرار دهید
3. فایل `index.html` را باز کنید و این خط:
   ```html
   <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
   ```
   را به این تغییر دهید:
   ```html
   <link rel="icon" type="image/png" href="/favicon.png" />
   ```
4. خط `apple-touch-icon` را هم به `/favicon.png` تغییر دهید

### روش ب — لوگوی SVG (کیفیت بهتر)
اگر فایل SVG لوگو را دارید، کافی است آن را با نام `favicon.svg` جایگزین فایل موجود در `public/` کنید — نیازی به تغییر کد نیست.

> 💡 اگر لوگو مستطیلی است، ابتدا در یک بوم مربعی قرار دهید تا در تب مرورگر بریده نشود.
> 💡 بعد از تغییر فاویکون، مرورگر ممکن است لوگوی قدیمی را نشان دهد — با `Ctrl + Shift + R` کش را پاک کنید.

---

## ۶️⃣ مرحله نهایی: ساخت نسخه نهایی (خیلی مهم!)

بعد از هر تغییر در عکس‌ها یا کد، حتماً این دستور را اجرا کنید تا فایل‌های نهایی ساخته شوند:

```bash
npm run build
```

سپس **کل محتویات پوشه `dist`** (هم فایل `index.html` و هم پوشه `images`) را روی هاست آپلود کنید.

> ⚠️ اگر فقط `index.html` را آپلود کنید و پوشه `images` را نه، عکس‌ها نمایش داده نمی‌شوند!

---

## 💡 نکات بهینه‌سازی عکس‌ها

- قبل از آپلود، عکس‌ها را با ابزارهایی مثل [squoosh.app](https://squoosh.app) یا [tinypng.com](https://tinypng.com) فشرده کنید تا سایت سریع لود شود
- حجم هر عکس بهتر است زیر ۳۰۰ کیلوبایت باشد (به‌جز عکس ۳۶۰ که می‌تواند تا ۱ مگابایت باشد)
- فرمت پیشنهادی: **JPG** برای عکس‌ها، **PNG** فقط برای لوگو
