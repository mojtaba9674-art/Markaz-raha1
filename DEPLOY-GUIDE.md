# 🚀 راهنمای کامل انتشار سایت مرکز رها روی اینترنت

این سایت **کاملاً استاتیک** است؛ یعنی به دیتابیس، بک‌اند یا سرور خاصی نیاز ندارد و روی هر هاست ساده‌ای کار می‌کند.

---

## قدم ۱: ساخت نسخه نهایی (Build)

هر بار که تغییری در سایت می‌دهید (عکس، محصول، متن و...) اول این دستور را اجرا کنید:

```bash
npm run build
```

بعد از اجرا، پوشه‌ای به نام `dist` ساخته می‌شود که محتویاتش این است:

```
dist/
├── index.html      ← کل سایت (همه‌چیز در یک فایل)
└── images/         ← پوشه عکس‌ها
    ├── gallery/
    ├── 360/
    ├── shop/
    └── team/
```

> ⚠️ **خیلی مهم:** هر دو مورد (فایل `index.html` **و** پوشه `images`) باید آپلود شوند؛ اگر پوشه images آپلود نشود، عکس‌ها نمایش داده نمی‌شوند.

---

## قدم ۲: خرید دامنه و هاست

### دامنه (دامین)
- **دامنه `.ir`:** از [nic.ir](https://nic.ir) — ارزان و رسمی (نیاز به کد ملی/شناسه ایرانیت دارد)
- پیشنهاد نام‌ها: `markazraha.ir` یا `raha-jiroft.ir` یا `markaz-raha.ir`
- **دامنه `.com`:** از ثبت‌کننده‌های ایرانی هم قابل خرید است

### هاست
برای این سایت، **ارزان‌ترین پلن هاست لینوکسی** کافی است (حتی ۱۰۰ مگابایت!). از شرکت‌های ایرانی مثل:
- میزبان‌فا، پارس‌هاست، ایران‌سرور، هاست‌نگار و...

هاست با کنترل‌پنل **cPanel** انتخاب کنید تا مراحل بعدی ساده باشد.

> 💡 معمولاً وقتی هاست می‌خرید، شرکت هاستینگ دامنه را به هاست متصل (DNS) می‌کند. کافی است در تیکت پشتیبانی بگویید «دامنه‌ام را به هاستم متصل کنید».

---

## قدم ۳: آپلود سایت روی cPanel

1. وارد **cPanel** هاست شوید (لینک و رمزش را شرکت هاستینگ می‌دهد)
2. روی **File Manager** کلیک کنید
3. وارد پوشه **`public_html`** شوید
4. اگر فایل پیش‌فرضی مثل `index.php` یا `default.html` هست، حذفش کنید
5. محتویات پوشه `dist` را **زیپ** کنید (هم index.html و هم پوشه images با هم)
6. در File Manager روی **Upload** بزنید و فایل زیپ را آپلود کنید
7. روی فایل زیپ راست‌کلیک و **Extract** را بزنید
8. بعد از Extract، مطمئن شوید فایل `index.html` **مستقیماً داخل** `public_html` است (نه داخل یک زیرپوشه اضافی)

✅ تمام! حالا دامنه را در مرورگر باز کنید؛ سایت بالا آمده است.

---

## قدم ۴: فعال‌سازی SSL رایگان (قفل سبز 🔒)

1. در cPanel وارد بخش **SSL/TLS Status** شوید
2. دامنه را انتخاب و دکمه **Run AutoSSL** را بزنید
3. بعد از چند دقیقه سایت با `https://` و قفل سبز باز می‌شود

این کار هم برای اعتماد کاربران و هم برای سئوی گوگل مهم است.

---

## 🆓 انتشار کاملاً رایگان (مرحله به مرحله)

### روش ۱: Netlify — ساده‌ترین راه (۵ دقیقه)

1. پروژه را دانلود و زیپ را Extract کنید؛ پوشه **`dist`** را پیدا کنید
2. به [app.netlify.com/drop](https://app.netlify.com/drop) بروید
3. یک حساب رایگان بسازید (Sign up با ایمیل — تا حساب نسازید سایت بعد از ۲۴ ساعت حذف می‌شود)
4. پوشه `dist` را **بکشید و روی صفحه رها کنید** (Drag & Drop)
5. چند ثانیه بعد سایت بالا می‌آید با آدرسی مثل `random-name.netlify.app`
6. برای تغییر نام: **Site settings → Change site name** → مثلاً `markazraha` بگذارید تا آدرس شود `markazraha.netlify.app`

> ⚠️ ممکن است برای باز کردن داشبورد Netlify از ایران به فیلترشکن نیاز باشد؛ اما بعد از آپلود، **سایت برای بازدیدکنندگان ایرانی بدون مشکل باز می‌شود**.

### روش ۲: GitHub Pages — بدون نیاز به فیلترشکن

1. در [github.com](https://github.com) یک حساب رایگان بسازید
2. روی **New repository** بزنید و نام ریپوزیتوری را **دقیقاً** این بگذارید: `USERNAME.github.io` (به جای USERNAME نام کاربری خودتان) — حتماً **Public** باشد
3. در صفحه ریپوزیتوری روی **Add file → Upload files** کلیک کنید
4. فایل `index.html` و پوشه `images` (از داخل dist) را بکشید و رها کنید → دکمه **Commit changes**
5. بروید به **Settings → Pages** → در بخش Branch گزینه **main** را انتخاب و **Save** کنید
6. بعد از ۱-۲ دقیقه سایت روی `USERNAME.github.io` بالا می‌آید ✅

> 💡 چون نام ریپوزیتوری `USERNAME.github.io` است، سایت از ریشه سرو می‌شود و عکس‌ها درست لود می‌شوند.

### روش ۳: Cloudflare Pages — سریع و پایدار

1. در [dash.cloudflare.com](https://dash.cloudflare.com) حساب رایگان بسازید
2. از منو: **Workers & Pages → Create → Pages → Upload assets**
3. نام پروژه: `markazraha` → پوشه `dist` را بکشید و رها کنید → **Deploy**
4. سایت روی `markazraha.pages.dev` بالا می‌آید ✅

### اتصال دامنه شخصی (بعداً، رایگان!)

وقتی دامنه خریدید (مثلاً markazraha.ir)، در هر سه سرویس بالا می‌توانید **رایگان** آن را وصل کنید: در تنظیمات سایت گزینه **Custom Domain** را بزنید و رکوردهای DNS که می‌دهد را در پنل دامنه‌تان وارد کنید. SSL رایگان هم خودکار فعال می‌شود.

---

## 🚨 مشکل: سایت روی pages.dev بدون فیلترشکن باز نمی‌شود

### چرا این اتفاق می‌افتد؟
دامنه‌های `*.pages.dev` و `*.workers.dev` **در ایران فیلتر شده‌اند**. مشکل از سایت یا هاست شما نیست — فقط همین نام دامنه مسدود است. خودِ سرورهای Cloudflare از ایران در دسترس هستند.

### ✅ راه‌حل قطعی: اتصال دامنه شخصی به همان پروژه

به‌محض اینکه یک دامنه (مثلاً `markazraha.ir`) به پروژه Cloudflare Pages وصل کنید، سایت **بدون فیلترشکن** برای همه باز می‌شود. سایت را جابه‌جا نمی‌کنید؛ فقط یک آدرس جدید به آن اضافه می‌شود.

**قدم ۱ — خرید دامنه `.ir`**
- به [nic.ir](https://nic.ir) بروید و ثبت‌نام کنید (نیاز به کد ملی / شناسه ایرانیت)
- دامنه مثلاً `markazraha.ir` را جستجو و ثبت کنید
- هزینه: بسیار ناچیز (حدود چند ده هزار تومان برای یک سال)

**قدم ۲ — افزودن دامنه به Cloudflare**
1. در [dash.cloudflare.com](https://dash.cloudflare.com) روی **Add a site** بزنید
2. دامنه‌تان را وارد و پلن **Free** را انتخاب کنید
3. Cloudflare دو آدرس Nameserver به شما می‌دهد؛ مثلاً:
   ```
   xxx.ns.cloudflare.com
   yyy.ns.cloudflare.com
   ```
   این دو را یادداشت کنید

**قدم ۳ — تنظیم Nameserver در پنل nic.ir**
1. وارد پنل [nic.ir](https://nic.ir) شوید → بخش **سرویس‌دهنده‌های DNS** دامنه
2. دو Nameserver مرحله قبل را وارد و ذخیره کنید
3. بین چند ساعت تا ۲۴ ساعت طول می‌کشد تا اعمال شود

**قدم ۴ — اتصال دامنه به پروژه Pages**
1. در Cloudflare: **Workers & Pages** → پروژه سایت → تب **Custom domains**
2. **Set up a domain** → دامنه‌تان را وارد کنید → **Activate**
3. Cloudflare خودش رکورد DNS و SSL را می‌سازد

---

## ⚡ حالت ویژه: دامنه از قبل در Cloudflare ثبت شده (سریع‌ترین راه — ۳ دقیقه)

اگر دامنه‌تان **همین الان داخل حساب Cloudflare** شماست (یعنی در لیست Websites داشبورد دیده می‌شود)، اتصال فقط چند کلیک است:

**قدم ۱ — رفتن به پروژه Pages**
1. وارد [dash.cloudflare.com](https://dash.cloudflare.com) شوید
2. از منوی سمت چپ: **Workers & Pages**
3. روی نام پروژه سایت (همان که با `.pages.dev` بالا آمده) کلیک کنید

**قدم ۲ — افزودن دامنه سفارشی**
1. تب **Custom domains** را باز کنید
2. دکمه **Set up a custom domain** را بزنید
3. دامنه‌تان را وارد کنید:
   - برای دامنه اصلی: `example.com`
   - یا برای زیردامنه: `www.example.com`
4. **Continue** → صفحه بعد رکورد DNS پیشنهادی را نشان می‌دهد
5. دکمه **Activate domain** را بزنید

**قدم ۳ — تمام!**
چون دامنه داخل همین حساب Cloudflare است، رکورد DNS به‌صورت **خودکار** ساخته می‌شود و نیازی به تنظیم دستی نیست.

- وضعیت اول: `Initializing` یا `Pending`
- بعد از **۱ تا ۵ دقیقه**: `Active` ✅
- گواهی SSL هم خودکار صادر می‌شود (گاهی تا ۱۵ دقیقه طول می‌کشد)

**قدم ۴ — تست**
دامنه را در مرورگر باز کنید. اگر خطای SSL دید، چند دقیقه صبر کنید تا گواهی صادر شود.

> 💡 **پیشنهاد:** هر دو نسخه `example.com` و `www.example.com` را اضافه کنید تا کاربر با هر دو حالت به سایت برسد.

### تنظیم اختیاری: انتقال خودکار www به دامنه اصلی
اگر می‌خواهید همه به `example.com` هدایت شوند:
**Rules → Redirect Rules → Create rule** → از `www.example.com` به `example.com` با کد ۳۰۱.

### به‌روزرسانی آدرس در فایل سئو
بعد از اتصال دامنه، در `index.html` همه‌جا `https://markazraha.ir/` را با دامنه واقعی خود عوض کنید، سپس `npm run build` بگیرید و پوشه `dist` جدید را در **Workers & Pages → پروژه → Create deployment** آپلود کنید.

### 🩺 عیب‌یابی سریع

| مشکل | راه‌حل |
|---|---|
| بعد از ۱۵ دقیقه هنوز Pending است | در تب **DNS** دامنه، مطمئن شوید رکورد `CNAME` با نام دامنه به `پروژه.pages.dev` وجود دارد و ابر نارنجی (Proxied) روشن است |
| خطای SSL / Not Secure | ۱۵ دقیقه صبر کنید؛ سپس **SSL/TLS → Overview** را روی **Full** بگذارید |
| سایت باز می‌شود ولی عکس‌ها نیستند | پوشه `images` همراه `index.html` آپلود نشده — دوباره کل محتویات `dist` را دیپلوی کنید |
| رکورد DNS قدیمی مزاحم است | در تب **DNS** رکورد `A` یا `CNAME` قبلیِ همان نام را حذف کنید و دوباره Activate بزنید |

**قدم ۵ — به‌روزرسانی آدرس در سئو**
در فایل `index.html` همه‌جا `https://markazraha.ir/` را با دامنه واقعی خود جایگزین کنید (در canonical و Schemaها)، سپس:
```bash
npm run build
```
و پوشه `dist` جدید را دوباره در Cloudflare Pages آپلود کنید.

✅ حالا سایت روی دامنه شخصی، **بدون فیلترشکن و با SSL رایگان** بالا می‌آید.

---

### 🔁 گزینه‌های جایگزین (اگر فعلاً نمی‌خواهید دامنه بخرید)

| گزینه | آدرس نمونه | وضعیت در ایران | هزینه |
|---|---|---|---|
| **GitHub Pages** | `username.github.io` | معمولاً بدون فیلترشکن باز می‌شود | رایگان |
| **هاست ایرانی** | دامنه خودتان | کاملاً بدون مشکل + سرعت بالا | ارزان |
| **لیارا / ابرآروان** | `markazraha.liara.run` | بدون فیلترشکن | رایگان تا ارزان |

> 💡 **توصیه نهایی:** برای یک مرکز درمانی که مخاطبش مردم جیرفت و ایران هستند، بهترین و حرفه‌ای‌ترین گزینه **دامنه `.ir` + هاست ایرانی یا Cloudflare با دامنه شخصی** است. هم بدون فیلترشکن باز می‌شود، هم سرعتش برای کاربر ایرانی بالاست، هم در گوگل بهتر دیده می‌شود.

---

## به‌روزرسانی سایت در آینده

هر وقت تغییری دادید (محصول جدید، عکس جدید، فروخته شدن اثر و...):

```bash
npm run build
```

سپس محتویات جدید پوشه `dist` را دوباره در `public_html` آپلود و جایگزین قبلی کنید. همین!

---

## 📌 نکات مهم قبل از انتشار

- **فرم تماس:** فرم «درخواست مشاوره» فعلاً فقط در سمت کاربر کار می‌کند و پیامی به جایی ارسال نمی‌کند (نمایش موفقیت نمایشی است). کانال‌های ارتباطی واقعی فعلی: **تلفن، واتساپ و اینستاگرام**. اگر خواستید فرم واقعاً پیام بفرستد، بعداً می‌توان سرویسی مثل Formspree به آن وصل کرد.
- **به‌روزرسانی آدرس سایت در سئو:** بعد از خرید دامنه، در فایل `index.html` همه جا `https://markazraha.ir/` را با دامنه واقعی خودتان عوض کنید (چند جا در تگ‌های Schema و Canonical است) و دوباره build بگیرید.
- **بدون نیاز به تنظیمات اضافه:** چون سایت تک‌صفحه‌ای است و منوها با `#` کار می‌کنند، نیازی به فایل `.htaccess` یا تنظیمات خاص سرور نیست.

---

## چک‌لیست نهایی انتشار ✅

- [ ] اجرای `npm run build`
- [ ] خرید دامنه و هاست
- [ ] آپلود `index.html` + پوشه `images` در `public_html`
- [ ] فعال‌سازی SSL رایگان
- [ ] تست سایت روی موبایل و کامپیوتر
- [ ] جایگزینی دامنه واقعی در فایل index.html و build مجدد
- [ ] معرفی سایت در گوگل: [search.google.com/search-console](https://search.google.com/search-console)
